spring:
  application:
    name: mallapi

  datasource:
      driver-class-name: org.mariadb.jdbc.Driver
      url: jdbc:mariadb://localhost:3306/malldb?characterEncoding=UTF-8&serverTimezone=UTC
      username: malladmin
      password: malladmin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true


